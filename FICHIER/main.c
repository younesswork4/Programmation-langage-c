#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "EX02.h"
/*
int main()
{
    int choix;
    do{
        printf("**********Menu gestion des Notes **************\n\n");
        printf("1-Nouvel etudiant\n");
        printf("2-Afficher Bulletin\n");
        printf("3-Modifier le nom\n");
        printf("4-supprimer un etudiant\n");
        printf("5-Afficher la moyenne de la classe\n");
        printf("6-Liste des Admis\n");
        printf("0-Fin de traitement\n");
        printf("Votre Choix:");scanf("%d",&choix);
        switch(choix){
        case 1 :{ajouterETD();break;}
        case 2 :{        ;break;}
        case 3 :{        ;break;}
        case 4 :{        ;break;}
        case 5 :{        ;break;}
        case 6 :{        ;break;}
        case 0 :{printf("Fin de traitement\n");break;}
        default : printf("Choix errone !!! le choix entre [0 - 6]\n\n");
        }
    }while(choix!=0);
    return 0;
}
























    /*Liste *L1=NULL;
    Etudiant E;
    FILE*fb;
    int chx;
    do{
        printf("Donner le nom:");
        fflush(stdin);
        gets(E.nom);
        printf("Donner le prenom:");
        fflush(stdin);
        gets(E.prenom);
        printf("Donner l'Age:");
        scanf("%d",&E.age);
        printf("Donner la moyenne:");
        scanf("%f",&E.moyennee);
        ajoutertete(&L1,E);
        printf("il ya un autre Etudiant si existe tape (0/1):");
        scanf("%d",&chx);
    }while(chx!=0);
    afficherListe(L1);
    stckeListe(L1,fb);*/



